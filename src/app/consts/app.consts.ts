export const Consts = {
    /**
     * It's too complicated to handle all edge cases, such as internationalization
     * and unicode characters, so we'll keep it simple and just check for the presence
     * of the "@" character.
     */
	EMAIL_REGEX: /@/
}